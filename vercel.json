{
  "version": 2,
  "installCommand": "npm install -g pnpm && pnpm install",
  "buildCommand": "pnpm -w run build:prod",
  "outputDirectory": "artifacts/forth-architecture/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/app" }
  ],
  "functions": {
    "api/app.mjs": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}
